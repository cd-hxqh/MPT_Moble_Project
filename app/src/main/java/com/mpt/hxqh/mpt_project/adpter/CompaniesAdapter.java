package com.mpt.hxqh.mpt_project.adpter;

import android.animation.Animator;
import android.content.Context;

import com.mpt.hxqh.mpt_project.R;
import com.mpt.hxqh.mpt_project.model.COMPANIES;
import com.mpt.hxqh.mpt_project.ui.widget.BaseViewHolder;

import java.util.List;


/**
 * Created by apple on 15/10/26
 * Companies适配器
 */
public class CompaniesAdapter extends BaseQuickAdapter<COMPANIES> {
    private int position;
    public CompaniesAdapter(Context context, int layoutResId, List data) {
        super(context, layoutResId, data);
    }

    @Override
    protected void startAnim(Animator anim, int index) {
        super.startAnim(anim, index);
        position = index;
        if (index < 5)
            anim.setStartDelay(index * 150);
    }

    @Override
    protected void convert(BaseViewHolder helper, COMPANIES item) {
        helper.setText(R.id.num_text_id, item.getCOMPANY());
        helper.setText(R.id.description_text, item.getNAME());
    }


}
